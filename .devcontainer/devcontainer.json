{
  "image": "python:3.11",
  "ports": [{
    "port": 8080,
    "label": "fastapi",
    "exposed": true
  }],
  "postCreateCommand": "pip install -r requirements.txt || pip install fastapi uvicorn[standard] sentence-transformers qdrant-client",
  "customizations": {
    "vs2": "Use to lance RETROKO de port en Codespaces"
  }
}